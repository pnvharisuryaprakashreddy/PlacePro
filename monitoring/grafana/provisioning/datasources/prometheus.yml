# Auto-provisions the Prometheus data source in Grafana so the demo stack
# works out of the box (no manual "Add data source" step needed).
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
